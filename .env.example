# Get a free key at https://console.groq.com/keys (no credit card required)
GROQ_API_KEY=your_groq_api_key_here

# Optional: lock CORS to your frontend origin in production.
# Leave unset to allow all origins (fine for a public demo API).
# CLIENT_ORIGIN=https://your-frontend.vercel.app

# Optional: the host usually sets this automatically.
# PORT=3000
